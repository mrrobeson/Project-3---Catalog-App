This is a web application built on the Python Flask framework.

The application offers users a way to model items within categories. Users may login using a Google username and create, edit, and delete items.

To run this application, clone this project, navigate to the catalog folder, and run project.py. By default, this app is accessible in a web browser at the address 'localhost:8000'.
